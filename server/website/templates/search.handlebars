<p>
<span>{{numberOfHits}}</span> hits{{#if hits}}:{{/if}}
</p>

{{#each hits}}
<p>
	<h1 class="title is-5">
		<span class="has-text-link"><a href="/browse.html?f={{fileId}}">
			{{filename}}</a></span>
	</h1>
	<h1 class="subtitle is-6"
		<span class="has-text-link"><a href="/browse.html?c={{certfp}}">
			{{hostname}}</a></span>
		<br>
		<span class="excerpt">{{{excerpt}}}</span>
	</h1>
</p>
{{/each}}
